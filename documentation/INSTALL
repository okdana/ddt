Installation instructions for `ddt`:

From the root project directory, run the following commands:

    ./configure && sudo make install

This will merge/build all of the source files and then install them to the
appropriate locations on your system.

That's all.


